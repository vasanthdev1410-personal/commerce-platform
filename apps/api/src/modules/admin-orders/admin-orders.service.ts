
import { BadRequestException, ConflictException, Injectable, NotFoundException, ServiceUnavailableException } from '@nestjs/common';
import { Prisma, type OrderStatus } from '../../generated/prisma/client';
import { PrismaService } from '../../database/prisma.service';
import { AdminAuditService } from '../admin-audit/admin-audit.service';
import { RazorpayService } from '../payments/razorpay.service';
import type { AdminOrderQueryDto, AdminPaymentQueryDto, RefundPaymentDto } from './dto/admin-order.dto';

const paymentSafe={id:true,provider:true,providerOrderId:true,providerPaymentId:true,status:true,amountPaise:true,createdAt:true,updatedAt:true} satisfies Prisma.PaymentSelect;
const transitions:Record<OrderStatus,OrderStatus[]>={PENDING:['CONFIRMED','CANCELLED'],CONFIRMED:['PROCESSING','CANCELLED'],PROCESSING:['SHIPPED'],SHIPPED:['DELIVERED'],DELIVERED:[],CANCELLED:[],REFUNDED:[]};
@Injectable()
export class AdminOrdersService{
constructor(private readonly prisma:PrismaService,private readonly razorpay:RazorpayService,private readonly audit:AdminAuditService){}
async orders(q:AdminOrderQueryDto){const where:Prisma.OrderWhereInput={status:q.status,pricingMode:q.pricingMode,...(q.paymentStatus&&{payments:{some:{status:q.paymentStatus}}}),...(q.search&&{OR:[{orderNumber:{contains:q.search,mode:'insensitive'}},{user:{email:{contains:q.search,mode:'insensitive'}}},{user:{phone:{contains:q.search,mode:'insensitive'}}}]}),...((q.dateFrom||q.dateTo)&&{createdAt:{gte:q.dateFrom?new Date(q.dateFrom):undefined,lte:q.dateTo?new Date(q.dateTo):undefined}})};const select={id:true,orderNumber:true,status:true,pricingMode:true,totalPaise:true,createdAt:true,updatedAt:true,user:{select:{id:true,email:true,phone:true,firstName:true,lastName:true}},payments:{select:paymentSafe}} satisfies Prisma.OrderSelect;const[data,total]=await this.prisma.$transaction([this.prisma.order.findMany({where,select,skip:(q.page-1)*q.limit,take:q.limit,orderBy:{createdAt:'desc'}}),this.prisma.order.count({where})]);return{data,pagination:{page:q.page,limit:q.limit,total,totalPages:Math.ceil(total/q.limit)}};}
async order(id:string){const row=await this.prisma.order.findUnique({where:{id},include:{user:{select:{id:true,email:true,phone:true,firstName:true,lastName:true,accountType:true}},items:true,payments:{select:{...paymentSafe,refunds:{select:{id:true,providerRefundId:true,amountPaise:true,reason:true,status:true,createdAt:true}}}},inventoryReservations:{select:{id:true,variantId:true,quantity:true,expiresAt:true,releasedAt:true}},events:{orderBy:{createdAt:'asc'}}}});if(!row)throw new NotFoundException('Order not found');const{shippingFullName,shippingPhone,shippingAddressLine1,shippingAddressLine2,shippingCity,shippingState,shippingPostalCode,shippingCountryCode,...safe}=row;return{...safe,shippingSnapshot:{fullName:shippingFullName,phone:shippingPhone,addressLine1:shippingAddressLine1,addressLine2:shippingAddressLine2,city:shippingCity,state:shippingState,postalCode:shippingPostalCode,countryCode:shippingCountryCode}};}
async changeStatus(adminId:string,id:string,next:OrderStatus){return this.prisma.$transaction(async tx=>{const order=await tx.order.findUnique({where:{id},include:{payments:true,inventoryReservations:{where:{releasedAt:null}}}});if(!order)throw new NotFoundException('Order not found');if(!transitions[order.status].includes(next))throw new ConflictException('Invalid order status transition');const paid=order.payments.some(p=>['PAID','PARTIALLY_REFUNDED'].includes(p.status));if(next==='CONFIRMED'&&!paid)throw new ConflictException('Unpaid order cannot be confirmed');if(next==='CANCELLED'&&paid)throw new ConflictException('Paid orders must use the refund workflow');if(next==='CANCELLED')for(const reservation of order.inventoryReservations){await tx.$executeRaw`UPDATE "Inventory" SET "reservedQuantity"=GREATEST(0,"reservedQuantity"-${reservation.quantity}) WHERE "variantId"=${reservation.variantId}::uuid`;await tx.inventoryReservation.update({where:{id:reservation.id},data:{releasedAt:new Date()}});}const updated=await tx.order.update({where:{id},data:{status:next}});const type=next==='CANCELLED'?'ORDER_CANCELLED':`ORDER_${next}`;await tx.orderEvent.create({data:{orderId:id,type,message:`Order status changed from ${order.status} to ${next}`,actorType:'ADMIN',actorUserId:adminId,metadata:{oldStatus:order.status,newStatus:next}}});await this.audit.record({adminUserId:adminId,action:next==='CANCELLED'?'ORDER_CANCEL_ADMIN':'ORDER_STATUS_CHANGE',entityType:'Order',entityId:id,metadata:{oldStatus:order.status,newStatus:next}},tx);return updated;},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable});}
async payments(q:AdminPaymentQueryDto){const where:Prisma.PaymentWhereInput={status:q.status,provider:q.provider?{equals:q.provider,mode:'insensitive'}:undefined,order:q.orderNumber?{orderNumber:{contains:q.orderNumber,mode:'insensitive'}}:undefined,...((q.dateFrom||q.dateTo)&&{createdAt:{gte:q.dateFrom?new Date(q.dateFrom):undefined,lte:q.dateTo?new Date(q.dateTo):undefined}})};const select={...paymentSafe,order:{select:{id:true,orderNumber:true,status:true,user:{select:{id:true,email:true}}}},refunds:{select:{id:true,amountPaise:true,status:true,createdAt:true}}} satisfies Prisma.PaymentSelect;const[data,total]=await this.prisma.$transaction([this.prisma.payment.findMany({where,select,skip:(q.page-1)*q.limit,take:q.limit,orderBy:{createdAt:'desc'}}),this.prisma.payment.count({where})]);return{data,pagination:{page:q.page,limit:q.limit,total,totalPages:Math.ceil(total/q.limit)}};}
async payment(id:string){const row=await this.prisma.payment.findUnique({where:{id},select:{...paymentSafe,order:{select:{id:true,orderNumber:true,status:true,totalPaise:true,createdAt:true}},refunds:{select:{id:true,providerRefundId:true,amountPaise:true,reason:true,status:true,requestedByAdminId:true,createdAt:true,updatedAt:true},orderBy:{createdAt:'asc'}}}});if(!row)throw new NotFoundException('Payment not found');return row;}
async refund(adminId:string,orderId:string,key:string|undefined,dto:RefundPaymentDto){if(!key||key.length<16||key.length>200)throw new BadRequestException('A valid Idempotency-Key is required');const prepared=await this.prisma.$transaction(async tx=>{await tx.$executeRaw`SELECT pg_advisory_xact_lock(hashtext(${orderId}))`;const payment=await tx.payment.findUnique({where:{orderId},include:{order:true,refunds:true}});if(!payment)throw new NotFoundException('Payment not found');const existing=payment.refunds.find(r=>r.idempotencyKey===key);if(existing)return{existing};if(!payment.providerPaymentId||!['PAID','PARTIALLY_REFUNDED'].includes(payment.status))throw new ConflictException('Payment is not refundable');const used=payment.refunds.filter(r=>r.status==='PROCESSED'||r.status==='PENDING').reduce((s,r)=>s+r.amountPaise,0),refundable=payment.amountPaise-used;if(dto.amountPaise>refundable)throw new ConflictException('Refund amount exceeds refundable balance');const created=await tx.paymentRefund.create({data:{paymentId:payment.id,amountPaise:dto.amountPaise,reason:dto.reason,idempotencyKey:key,requestedByAdminId:adminId}});await tx.orderEvent.create({data:{orderId,type:'REFUND_REQUESTED',message:'Refund requested',actorType:'ADMIN',actorUserId:adminId,metadata:{refundId:created.id,amountPaise:dto.amountPaise}}});await this.audit.record({adminUserId:adminId,action:'REFUND_CREATE',entityType:'PaymentRefund',entityId:created.id,metadata:{paymentId:payment.id,orderId,amountPaise:dto.amountPaise}},tx);return{created,payment};},{isolationLevel:Prisma.TransactionIsolationLevel.ReadCommitted});if('existing'in prepared)return prepared.existing;try{const provider=await this.razorpay.refundProviderPayment(prepared.payment.providerPaymentId!,dto.amountPaise,prepared.created.id);if(provider.status!=='processed'||provider.amount!==dto.amountPaise)throw new ServiceUnavailableException('Refund was not confirmed by provider');return await this.prisma.$transaction(async tx=>{const refund=await tx.paymentRefund.update({where:{id:prepared.created.id},data:{status:'PROCESSED',providerRefundId:provider.id}});const sums=await tx.paymentRefund.aggregate({where:{paymentId:prepared.payment.id,status:'PROCESSED'},_sum:{amountPaise:true}}),total=sums._sum.amountPaise||0,full=total===prepared.payment.amountPaise;await tx.payment.update({where:{id:prepared.payment.id},data:{status:full?'REFUNDED':'PARTIALLY_REFUNDED'}});if(full)await tx.order.update({where:{id:orderId},data:{status:'REFUNDED'}});await tx.orderEvent.create({data:{orderId,type:'REFUND_COMPLETED',message:full?'Payment fully refunded':'Payment partially refunded',actorType:'ADMIN',actorUserId:adminId,metadata:{refundId:refund.id,amountPaise:refund.amountPaise,totalRefundedPaise:total}}});await this.audit.record({adminUserId:adminId,action:'REFUND_COMPLETE',entityType:'PaymentRefund',entityId:refund.id,metadata:{paymentId:prepared.payment.id,orderId,amountPaise:refund.amountPaise,totalRefundedPaise:total}},tx);return{...refund,totalRefundedPaise:total,refundablePaise:prepared.payment.amountPaise-total,paymentStatus:full?'REFUNDED':'PARTIALLY_REFUNDED',orderStatus:full?'REFUNDED':prepared.payment.order.status};});}catch(error){await this.prisma.$transaction(async tx=>{await tx.paymentRefund.update({where:{id:prepared.created.id},data:{status:'FAILED'}});await tx.orderEvent.create({data:{orderId,type:'REFUND_FAILED',message:'Refund provider request failed',actorType:'ADMIN',actorUserId:adminId,metadata:{refundId:prepared.created.id,amountPaise:dto.amountPaise}}});await this.audit.record({adminUserId:adminId,action:'REFUND_FAILED',entityType:'PaymentRefund',entityId:prepared.created.id,metadata:{paymentId:prepared.payment.id,orderId,amountPaise:dto.amountPaise}},tx);});throw error;}}
async reconcile(adminId:string,paymentId:string){const before=await this.prisma.payment.findUnique({where:{id:paymentId},select:{status:true,orderId:true}});if(!before)throw new NotFoundException('Payment not found');const result=await this.razorpay.reconcilePayment(paymentId);await this.prisma.$transaction(async tx=>{if(result.changed)await tx.orderEvent.create({data:{orderId:before.orderId,type:'PAYMENT_CONFIRMED',message:'Payment reconciled with provider',actorType:'ADMIN',actorUserId:adminId,metadata:{oldStatus:before.status,newStatus:result.paymentStatus}}});await this.audit.record({adminUserId:adminId,action:'PAYMENT_RECONCILE',entityType:'Payment',entityId:paymentId,metadata:{oldStatus:before.status,newStatus:result.paymentStatus,changed:result.changed}},tx);});return result;}
}

