'use client';
import{useCallback,useState}from'react';export interface CurrentLocation{latitude:number;longitude:number;accuracy:number}export function useCurrentLocation(){const[location,setLocation]=useState<CurrentLocation|null>(null),[error,setError]=useState<string|null>(null),[loading,setLoading]=useState(false);const request=useCallback(()=>{if(!navigator.geolocation){setError('Location is not supported by this browser.');return}setLoading(true);setError(null);navigator.geolocation.getCurrentPosition(p=>{setLocation({latitude:p.coords.latitude,longitude:p.coords.longitude,accuracy:p.coords.accuracy});setLoading(false)},e=>{setError(e.code===e.PERMISSION_DENIED?'Location permission was denied.':e.code===e.TIMEOUT?'Location request timed out.':'Current location is unavailable.');setLoading(false)},{enableHighAccuracy:false,timeout:10000,maximumAge:60000})},[]);return{location,error,loading,request}}
