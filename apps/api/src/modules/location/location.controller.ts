import{Body,Controller,Get,Post,Query}from'@nestjs/common';import{Throttle}from'@nestjs/throttler';import{AutocompleteQueryDto,GeocodeDto,ReverseGeocodeDto}from'./dto/location.dto';import{LocationService}from'./location.service';@Controller('location')@Throttle({default:{limit:30,ttl:60000}})export class LocationController{constructor(private readonly s:LocationService){}@Get('autocomplete')autocomplete(@Query()q:AutocompleteQueryDto){return this.s.autocomplete(q.input,q.country,q.latitude,q.longitude,q.sessionToken)}@Post('geocode')geocode(@Body()d:GeocodeDto){return this.s.geocode(d.address)}@Post('reverse-geocode')reverse(@Body()d:ReverseGeocodeDto){return this.s.reverseGeocode(d.latitude,d.longitude)}}
