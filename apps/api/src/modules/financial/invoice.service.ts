import{ConflictException,Injectable,NotFoundException}from'@nestjs/common';import{ConfigService}from'@nestjs/config';import{randomBytes}from'node:crypto';import{PrismaService}from'../../database/prisma.service';import{AdminAuditService}from'../admin-audit/admin-audit.service';@Injectable()export class InvoiceService{private readonly seller:{legalName:string;gstin:string|null;state:string|null;billingAddress:string};constructor(private readonly prisma:PrismaService,config:ConfigService,private readonly audit:AdminAuditService){this.seller={legalName:config.get<string>('SELLER_LEGAL_NAME')||'Store',gstin:config.get<string>('SELLER_GSTIN')||null,state:config.get<string>('SELLER_STATE')||null,billingAddress:config.get<string>('SELLER_BILLING_ADDRESS')||''}}customer(userId:string,orderId:string){return this.get(orderId,userId)}admin(orderId:string,adminId:string){return this.get(orderId,undefined,adminId)}private async get(orderId:string,userId?:string,adminId?:string){const order=await this.prisma.order.findFirst({where:{id:orderId,...(userId&&{userId})},include:{user:{select:{email:true,phone:true,firstName:true,lastName:true}},items:true,payments:true,invoice:true}});if(!order)throw new NotFoundException('Invoice not found');if(!order.payments.some(p=>['PAID','PARTIALLY_REFUNDED','REFUNDED'].includes(p.status)))throw new ConflictException('Invoice is unavailable for an unpaid order');if(order.invoice)return order.invoice;const sellerSnapshot=this.seller,buyerSnapshot={email:order.user.email,phone:order.shippingPhone,name:order.shippingFullName,addressLine1:order.shippingAddressLine1,addressLine2:order.shippingAddressLine2,city:order.shippingCity,state:order.shippingState,postalCode:order.shippingPostalCode,countryCode:order.shippingCountryCode},financialSnapshot={orderNumber:order.orderNumber,items:order.items.map(i=>({sku:i.sku,productName:i.productName,variantName:i.variantName,quantity:i.quantity,unitPricePaise:i.unitPricePaise,totalPricePaise:i.totalPricePaise})),subtotalPaise:order.subtotalPaise,couponCode:order.couponCode,discountPaise:order.discountPaise,taxablePaise:order.taxablePaise,cgstPaise:order.cgstPaise,sgstPaise:order.sgstPaise,igstPaise:order.igstPaise,taxPaise:order.taxPaise,shippingPaise:order.shippingPaise,totalPaise:order.totalPaise};const invoice=await this.prisma.invoice.upsert({where:{orderId},update:{},create:{orderId,invoiceNumber:`INV-${new Date().getUTCFullYear()}-${randomBytes(8).toString('hex').toUpperCase()}`,sellerSnapshot,buyerSnapshot,financialSnapshot}});if(adminId)await this.audit.record({adminUserId:adminId,action:'INVOICE_CREATE',entityType:'Invoice',entityId:invoice.id,metadata:{orderId,invoiceNumber:invoice.invoiceNumber}});return invoice}}
