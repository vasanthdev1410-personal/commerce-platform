import{Inject,Injectable}from'@nestjs/common';import type{LocationProvider}from'./location.types';export const LOCATION_PROVIDER=Symbol('LOCATION_PROVIDER');@Injectable()export class LocationService{constructor(@Inject(LOCATION_PROVIDER)private readonly p:LocationProvider){}autocomplete(input:string,country:string,lat?:number,lng?:number,token?:string){return this.p.autocomplete(input,country,lat,lng,token)}geocode(address:string){return this.p.geocode(address)}reverseGeocode(lat:number,lng:number){return this.p.reverseGeocode(lat,lng)}}
